# SpectralFormer Reproduction Report (Seed 25873) - 20/01/2026

## 1. Objective
Thực hiện tái lập (Reproduction) nghiêm ngặt các thí nghiệm đã chạy trên UNet (Seed 25873) bằng kiến trúc **SpectralFormer (SF)**.
Mục tiêu là so sánh công bằng hiệu năng giữa CNN-based (UNet) và Transformer-based (SF) trong điều kiện tài nguyên giới hạn (**Lightweight ~0.5M params**).

---

## 2. Chi tiết thay đổi Code (Git-style Diff)

### 2.1. Đồng bộ hóa Data Pipeline
**File:** `SF/src/datasets.py`
Đã mở khóa các tính năng Augmentation và sửa lỗi mapping file nhãn.

```diff
--- SF/src/datasets.py
+++ SF/src/datasets.py
@@ -50,9 +50,31 @@
-        else: # Default, backward-compatible behavior
+        else: # Default, configurable behavior
             print("Using 'default' configurable augmentation pipeline.")
             aug_cfg = config.get('augmentation', {})
-            # ... (logic cũ giữ nguyên)
             transforms_list.extend([A.HorizontalFlip(p=0.5), A.RandomRotate90(p=0.5)])
+
+            if 'shift_scale_rotate' in aug_cfg:
+                p_ssr = aug_cfg['shift_scale_rotate'].get('p', 0.5)
+                if p_ssr > 0:
+                    transforms_list.append(A.ShiftScaleRotate(
+                        shift_limit=aug_cfg['shift_scale_rotate'].get('shift_limit', 0.0625),
+                        scale_limit=aug_cfg['shift_scale_rotate'].get('scale_limit', 0.1),
+                        rotate_limit=aug_cfg['shift_scale_rotate'].get('rotate_limit', 45),
+                        border_mode=cv2.BORDER_REFLECT_101, p=p_ssr))
+
+            if 'random_brightness_contrast' in aug_cfg:
+                p_rbc = aug_cfg['random_brightness_contrast'].get('p', 0.5)
+                if p_rbc > 0:
+                    transforms_list.append(A.RandomBrightnessContrast(p=p_rbc))
@@ -120,7 +142,8 @@
-        potential_names = [base_name, base_name.replace('_RC_TC', ''), base_name.replace('_RC', '')]
+        potential_names = [base_name, base_name.replace('_RC_TC', ''), 
+                          base_name.replace('_RC', ''), base_name.replace('_TC', '')]
```

### 2.2. Porting Learnable Band Selector (TABS Method)
**File:** `SF/models/base_blocks.py`
Đã thêm module chọn band có khả năng học được (Gumbel-Softmax STE).

```diff
--- SF/models/base_blocks.py
+++ SF/models/base_blocks.py
+class LearnableBandSelector(nn.Module):
+    def __init__(self, in_channels: int, num_bands_to_select: int):
+        super().__init__()
+        self.logits = nn.Parameter(torch.randn(in_channels))
+        self.temperature = 1.0
+        self.k = num_bands_to_select
+
+    def forward(self, x: torch.Tensor) -> torch.Tensor:
+        # Gumbel-Softmax STE Sampling logic...
+        # selection_mask, top_k_indices = self._gumbel_topk_sampling(...)
+        # return torch.index_select(x * mask, dim=1, index=top_k_indices)
```

### 2.3. Tích hợp Selector vào SpectralFormer
**File:** `SF/models/spectral_former_seg.py`
Chỉnh sửa kiến trúc để hỗ trợ chọn band đầu vào.

```diff
--- SF/models/spectral_former_seg.py
+++ SF/models/spectral_former_seg.py
@@ -93,1 +93,2 @@
-    def __init__(self, ..., in_channels=25, num_classes=5, patch_size=16, ...):
+    def __init__(self, ..., in_channels=25, num_selected_bands=None, ...):
+        # Khởi tạo band_selector nếu num_selected_bands < in_channels
@@ -153,1 +153,4 @@
     def forward(self, x):
+        if self.band_selector is not None:
+            x = self.band_selector(x)
```

### 2.4. Temperature Annealing cho Trainer
**File:** `SF/src/trainer.py`
Đảm bảo nhiệt độ Gumbel giảm dần để việc chọn band hội tụ từ "mềm" sang "cứng".

```diff
--- SF/src/trainer.py
+++ SF/src/trainer.py
@@ -109,6 +109,13 @@
         for epoch in range(self.start_epoch, self.num_epochs):
+            if hasattr(self.model, 'band_selector') and self.model.band_selector is not None:
+                new_tau = 5.0 * np.exp(-0.03912 * epoch) if epoch < 100 else 0.1
+                self.model.band_selector.temperature = new_tau
```

---

## 3. Ma trận Thí nghiệm (18 Jobs)
**Script:** `SF/scripts/reproduce_sf_seed_25873.py`

| Phương pháp | K-values | Kiến trúc | Params | Trạng thái |
| :--- | :--- | :--- | :--- | :--- |
| **TABS** | 3, 5, 7 | SF Tiny | ~0.45M | **Đang chạy** |
| **SVM-RFE** | 3, 5, 7 | SF Tiny | ~0.42M | **Đang chạy** |
| **Entropy** | 3, 5, 7 | SF Tiny | ~0.42M | **Đang chạy** |
| **Fisher** | 3, 5, 7 | SF Tiny | ~0.42M | **Đang chạy** |
| **Correlation** | 3, 5, 7 | SF Tiny | ~0.42M | **Đang chạy** |
| **SAD** | 3, 5, 7 | SF Tiny | ~0.42M | **Đang chạy** |

*Ghi chú: Model TABS có params cao hơn một chút (~0.45M) do chứa thêm vector Logits của Band Selector.*

---

## 4. Cấu hình SF Tiny (Chi tiết)
Để ép model Transformer xuống mức **0.46M params**, tôi đã sử dụng cấu hình:
*   `patch_size`: 16 (Giảm độ dài sequence)
*   `dim`: 64
*   `depth`: 3
*   `heads`: 4
*   `mlp_dim`: 128 (2x dim)
*   `LR`: 5e-4 (Phù hợp cho model nhỏ)
